

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
// new line
model User {
  id    Int     @default(autoincrement()) @id
  email String  @unique
  name  String
  
}